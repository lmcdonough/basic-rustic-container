# This gitignore file is suitible for Rust projects, and is configured to ignore all Rust compiled files and executables, cargo lock files, PostgreSQL data files, Docker files, and JetBrains IDE files.

# Generated by Cargo
# will have compiled files and executables
debug/
target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# RustRover
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# Docker specific ignores
docker-compose.override.yml
docker-compose.override.yaml
Dockerfile.*

# macOS specific ignores
.DS_Store

# Linux specific ignores
*~

# Node specific ignores (if using Node.js for any part of the project)
node_modules/

# Logs
*.log

# Temporary files
*.tmp
*.temp

# Compiled object files
*.o
*.so
*.dylib

# Binaries
*.exe
*.out
*.app

# Coverage files
*.lcov
coverage/

# IDE specific ignores
.vscode/
.idea/
*.iml

# Rust specific ignores
*.rlib
# PostgreSQL specific ignores
pgdata/
*.pgpass

# Docker specific ignores for PostgreSQL
docker-compose.yml
.env